#
chain @_acpt$chsuff @$chsuff
proto @

rule chain($chsuff) target(@$chsuff) lit(--icmp-type any)
cmp -A $chsuff -m icmp -p icmp  --icmp-type any -j icmp$chsuff
#----------------------------------------

target @_acpt$chsuff

include nmap ping

dst - ; src -

chain drop$chsuff @$chsuff
rule target(drop$chsuff) proto(-)
cmp -A icmp2lan -j drop2lan

include acpt
