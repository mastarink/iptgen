chain acpt$chsuff icmp_acpt$chsuff

rule target(ACCEPT) src($iplan) icmp-type(8)
cmp -A icmp_acpt2lan -m icmp -p icmp --icmp-type 8 -s $iplan -j ACCEPT
# -A icmp_acpt2lan -s $LAN_IP_RANGE -m icmp --icmp-type 8 -p icmp -j ACCEPT

rule target(acpt2lan) proto(-)
cmp -A icmp_acpt2lan -j acpt2lan
# -A icmp_acpt2lan -j acpt2lan

